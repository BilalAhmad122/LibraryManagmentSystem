@model IEnumerable<Data_Base_Layer.BookReturnTable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IssueDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReturenDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CurentDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookTable.BookType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeTable.FullName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserTable.UserName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IssueDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReturenDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CurentDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BookTable.BookType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeTable.FullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UserTable.UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookReturnID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookReturnID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookReturnID })
        </td>
    </tr>
}

</table>
